// Задача 5: Динамическое создание свойств
// Условие:
// Создайте объект config, где:

// Ключ theme берется из переменной currentTheme = "dark"

// Ключ version берется из переменной appVersion = "1.0.0"