Задача 5: Автоматическая синхронизация свойств объекта
Теоретическая подсказка:

Геттеры (get) позволяют динамически вычислять значение свойства при обращении к нему.

Сеттеры (set) автоматически обновляют связанные свойства при изменении значения.

Использование _property (подчеркивание) — соглашение для обозначения "внутреннего" свойства, которое не должно изменяться напрямую.

Условие задачи:
У вас есть объект circle, который должен хранить радиус (radius) и диаметр (diameter).

Без сеттера и геттера:

При изменении radius нужно вручную пересчитывать diameter.

При изменении diameter нужно вручную пересчитывать radius.

Это неудобно и может привести к ошибкам, если забыть обновить одно из свойств.

Сеттер + геттер:

Реализуйте автоматическую синхронизацию:

При изменении radius диаметр (diameter) должен вычисляться автоматически (diameter = radius * 2).

При изменении diameter радиус (radius) должен обновляться автоматически (radius = diameter / 2).

Пример работы:
javascript
const circle = {
  // Ваш код с геттерами и сеттерами
};

// Тестирование:
circle.radius = 10;
console.log(circle.diameter); // Должно быть 20 (автоматически вычислено)

circle.diameter = 30;
console.log(circle.radius); // Должно быть 15 (автоматически обновлено)
Решение:
javascript
const circle = {
  _radius: 0, // Внутреннее свойство

  get radius() {
    return this._radius;
  },

  set radius(value) {
    this._radius = value;
  },

  get diameter() {
    return this._radius * 2;
  },

  set diameter(value) {
    this._radius = value / 2;
  }
};
Вывод:
При изменении radius → diameter пересчитывается автоматически (при обращении через геттер).

При изменении diameter → radius обновляется через сеттер.

Больше не нужно вручную синхронизировать значения!

Разница:

Без сеттера/геттера — риск несогласованности данных.

С сеттером/геттером — данные всегда актуальны.

🚀 Доп. задание:
Добавьте свойство area (площадь круга), которое будет вычисляться как Math.PI * this._radius ** 2 и обновляться при изменении radius или diameter.